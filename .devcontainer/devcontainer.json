{
    "name": "Windows",
    "dockerComposeFile": "compose.yml",
    "service": "windows",
    // This is the line we're changing. 
    // We're pointing to a real workspace folder instead of /dev/null
    "workspaceFolder": "/workspace",

    "forwardPorts": [8006],
    "onAutoForward": "openBrowser"
}
